```bash
mkdir -p /data/milvus_knowledge_server


注意：如果  --network host 会导致在其他服务器无法telnet 8005端口
docker run -itd \
  --name py310-milvus-server \
  -p 8005:8005 \
  --gpus all \
  -v /data/milvus_knowledge_server:/workspace \
  -w /workspace \
  -e TZ=Asia/Shanghai \
  python:3.10-slim \
  /bin/bash
```